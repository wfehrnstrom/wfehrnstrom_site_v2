---
import { Image } from 'astro:assets';

import myPicture from '../resources/images/me-cropped.jpg';
import githubLogo from '../../public/logos/github-mark.png';
import linkedinLogo from '../../public/logos/linkedinlogo-bw.png';
import spotifyLogo from '../../public/logos/spotify-logo-bw.png';

interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Astro description" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body>
		<div class="header-page">
			<div class="header-wrapper">
				<div class="header-menu-container">
					<h1 class="title-name">Will Fehrnstrom</h1>
					<ul class="header-navigation-menu">
						<li>About Me</li>
						<li>Resume</li>
					</ul>
					<div class="external-links">
						<a href="https://github.com/wfehrnstrom"><Image src={githubLogo} alt="Github Octocat" loading="eager"/></a>
						<a href="https://www.linkedin.com/in/william-f-aba4a5119/"><Image src={linkedinLogo} 			alt="LinkedIn Symbol" loading="eager"/></a>
						<a href="https://open.spotify.com/playlist/4vkeZxk3irDgA88vB26ljg?si=db99d76f4f3c4093"><Image src={spotifyLogo} alt="Spotify Symbol" loading="eager"/></a>
					</div>
				</div>
				<div class="my-image-container">
					<Image class="my-image" src={myPicture} alt="me in Central Park with the New York Skyline in the background" object-fit="cover" loading="eager"/>
				</div>
			</div>		
		</div>
	</body>
</html>
<style is:global>
	@import '../styles/main.css';

	:root {
		--accent: rgb(150, 150, 150);
		--accent-light: rgb(238, 238, 238);
		--accent-dark: rgb(49, 49, 49);
		--accent-gradient: linear-gradient(
			45deg,
			rgb(var(--accent)),
			rgb(var(--accent-light)) 30%,
			white 60%
		);
	}
	html {
		font-family: 'Manrope', system-ui, sans-serif;
		background: #ffffff;
		background-color: 
	}
	code {
		font-family:
			
	}

	img {
		display: block;
		margin: auto;
		height: 100%;
	}
</style>

<style>
	.header-page {
		width: 100vw;
		height: 100vh;
	}

	.header-wrapper {
		width: 90vw;
		position: relative;
		display: flex;
		top: 50%;
		left: 50%;
		transform: translate(-50%, -50%);
	}

	.header-menu-container {
		flex: 3 1 auto;
		margin-right: 2rem;
	}

	.title-name {
		font-size: 3rem;
		margin-bottom: 0;
	}

	.header-navigation-menu {
		display: inline-flex;
		list-style-type: none;
		margin: 1.89rem 0 0 1.26rem;
		padding: 0;
	}

	.header-navigation-menu > li {
		margin: 0 7.13rem 0 0;
		width: 5rem;
	}

	.header-navigation-menu > li:first-of-type {
		padding-left: 0;
	}

	.external-links {
		display: flex;
		margin: 3rem 0 0 1.26rem;
	}

	.external-links a {
		display: block;
		width: 2.13rem;
		margin: 0;
		margin-right: 10rem;
	}

	.external-links img {
		object-fit: contain;
		width: 100%;
	}

	.external-links img:last-of-type {
		margin-right: 0;
	}

	.my-image-container {
		position: relative;
		flex: 1 3 26rem;
		margin: 0;
		padding: 0;
		width: 26rem;
		height: 26rem;
	}

	.my-image {
		position: absolute;
		left: 0;
		top: 0;
		display: block;
		margin-left: 0;
		width: 100%;
		object-fit: contain;
	}
</style>
